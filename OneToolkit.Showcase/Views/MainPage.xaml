<UserControl
	x:Class="OneToolkit.Showcase.Views.MainPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:animation="using:OneToolkit.UI.Xaml.Media.Animation"
	xmlns:controls="using:OneToolkit.UI.Xaml.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:OneToolkit.Showcase.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
	xmlns:triggers="using:Microsoft.Toolkit.Uwp.UI.Triggers"
	xmlns:viewModels="using:OneToolkit.Showcase.ViewModels"
	Loaded="UserControl_Loaded"
	mc:Ignorable="d">

	<UserControl.KeyboardAccelerators>
		<KeyboardAccelerator
			Key="Left"
			Invoked="BackAccelerator_Invoked"
			Modifiers="Menu" />
		<KeyboardAccelerator
			Key="Left"
			Invoked="BackClearAccelerator_Invoked"
			Modifiers="Menu, Shift" />
	</UserControl.KeyboardAccelerators>

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="{x:Bind viewModels:SettingsViewModel.Instance.TitleBarHeight, Mode=OneWay}" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid Grid.Row="0">
			<Grid
				x:Name="TitleBar"
				Margin="{x:Bind viewModels:SettingsViewModel.Instance.TitleBarLeftInset, Mode=OneWay}"
				Background="Transparent">
				<StackPanel
					Margin="12,0,0,0"
					HorizontalAlignment="Left"
					VerticalAlignment="Center"
					Orientation="Horizontal"
					Spacing="8">
					<Image
						Width="16"
						Height="16"
						VerticalAlignment="Center"
						Source="ms-appx:///Assets/Square150x150Logo.scale-200.png" />
					<TextBlock
						VerticalAlignment="Center"
						Style="{ThemeResource CaptionTextBlockStyle}"
						Text="OneToolkit Showcase" />
				</StackPanel>
			</Grid>
			<ComboBox
				x:Name="NavigationBox"
				x:Uid="NavigationBox"
				Margin="{x:Bind viewModels:SettingsViewModel.Instance.TitleBarRightInset, Mode=OneWay}"
				HorizontalAlignment="Right"
				VerticalAlignment="Center"
				SelectionChanged="NavigationBox_SelectionChanged">
				<ComboBox.Resources>
					<ResourceDictionary Source="ms-appx:///Microsoft.UI.Xaml/DensityStyles/Compact.xaml" />
				</ComboBox.Resources>
				<ComboBoxItem x:Uid="HomeNavItem" Tag="OneToolkit.Showcase.Views.HomePage" />
				<ComboBoxItem x:Uid="ReferenceNavItem" Tag="OneToolkit.Showcase.Views.ApiReference" />
				<ComboBoxItem x:Uid="SettingsNavItem" Tag="OneToolkit.Showcase.Views.SettingsPage" />
			</ComboBox>
		</Grid>
		<controls:ContentFrame
			x:Name="Presenter"
			Grid.Row="1"
			Background="{ThemeResource LayerFillColorDefaultBrush}"
			ContentChanged="Presenter_ContentChanged">
			<controls:ContentFrame.ContentTransition>
				<animation:SlideContentTransition />
			</controls:ContentFrame.ContentTransition>
		</controls:ContentFrame>
	</Grid>

	<!--<controls:PageView
			x:Name="NavView"
			IsBackButtonVisible="Collapsed"
			MenuItemInvoked="NavView_MenuItemInvoked"
			PaneTitle="OneToolkit Showcase">
			<controls:PageView.ContentTransition>
				<animation:SlideContentTransition />
			</controls:PageView.ContentTransition>
			<controls:PageView.AutoSuggestBox>
				<AutoSuggestBox PlaceholderText="What are you looking for?" QueryIcon="Find" />
			</controls:PageView.AutoSuggestBox>
			<controls:PageView.SettingsContent>
				<local:SettingsPage />
			</controls:PageView.SettingsContent>
			<controls:PageView.MenuItems>
				<controls:PageViewItem x:Uid="HomeNavItem" Icon="Home">
					<local:HomePage />
				</controls:PageViewItem>
				<controls:PageViewItem Label="API reference" MenuItemsSource="{x:Bind viewModels:ReferenceViewModel.BindableNamespaces}">
					<controls:PageViewItem.Icon>
						<FontIcon Glyph="&#xE736;" />
					</controls:PageViewItem.Icon>
					<local:ApiReference />
				</controls:PageViewItem>
			</controls:PageView.MenuItems>
			<controls:PageView.FooterMenuItems>
				<muxc:NavigationViewItem x:Name="FeedbackNavItem" x:Uid="FeedbackNavItem">
					<muxc:NavigationViewItem.Icon>
						<FontIcon Glyph="&#xED15;" />
					</muxc:NavigationViewItem.Icon>
				</muxc:NavigationViewItem>
			</controls:PageView.FooterMenuItems>
		</controls:PageView>
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup>
				<VisualState>
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="{StaticResource MinWindowBreakpoint}" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="NavView.PaneDisplayMode" Value="LeftCompact" />
						<Setter Target="NavView.IsPaneToggleButtonVisible" Value="True" />
					</VisualState.Setters>
				</VisualState>
				<VisualState>
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="{StaticResource LargeWindowBreakpoint}" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="NavView.PaneDisplayMode" Value="Left" />
						<Setter Target="NavView.IsPaneToggleButtonVisible" Value="False" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
			<VisualStateGroup>
				<VisualState>
					<VisualState.StateTriggers>
						<triggers:UserInteractionModeStateTrigger InteractionMode="Mouse" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="NavView.IsBackButtonVisible" Value="Visible" />
					</VisualState.Setters>
				</VisualState>
				<VisualState>
					<VisualState.StateTriggers>
						<triggers:UserInteractionModeStateTrigger InteractionMode="Touch" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="NavView.IsBackButtonVisible" Value="Collapsed" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>-->
</UserControl>
