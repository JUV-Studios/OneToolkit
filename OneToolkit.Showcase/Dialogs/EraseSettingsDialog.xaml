<ContentDialog
	x:Class="OneToolkit.Showcase.Dialogs.EraseSettingsDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:viewModels="using:OneToolkit.Showcase.ViewModels"
	x:Uid="EraseSettingsDialog"
	DefaultButton="Primary"
	PrimaryButtonClick="{x:Bind viewModels:SettingsViewModel.Instance.EraseSettings}"
	SecondaryButtonText="{Binding Source='CancelText', Converter={StaticResource ResourceLocalizationConverter}}"
	Style="{StaticResource DefaultContentDialogStyle}"
	mc:Ignorable="d" />